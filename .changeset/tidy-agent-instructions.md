---
"@danieljvdm/dev-kit": minor
---

Manage Dev Kit and Vite+ instruction sections within an existing `AGENTS.md`, preserving handwritten project guidance across updates and cleanup. Generate an opinionated command policy that routes Vite+ projects exclusively through `vp` and runs declared scripts in non-Vite+ projects through Bun, independently of the dependency installer.
