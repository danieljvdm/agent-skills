interface:
  display_name: "Effect Expert"
  short_description: "Apply current Effect v4 patterns and tooling"
  default_prompt: "Use $effect-ts to apply current Effect v4 patterns for schemas, services, layers, errors, DateTime, testing, SQL, observability, or CLI workflows."
